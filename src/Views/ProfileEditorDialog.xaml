<Window x:Class="AppPilot.Views.ProfileEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:AppPilot.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:ProfileEditorViewModel}"
        Title="{Binding Title}"
        Width="820"
        Height="620"
        MinWidth="700"
        MinHeight="500"
        MaxHeight="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Height, Converter={StaticResource WorkAreaHeightConverter}}"
        Background="{DynamicResource WindowBg}"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        WindowStyle="SingleBorderWindow"
        Loaded="Window_Loaded">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- Converter for max height to respect small screens -->
        <local:WorkAreaHeightConverter x:Key="WorkAreaHeightConverter" xmlns:local="clr-namespace:AppPilot.Converters"/>

        <!-- Scrollbar styles -->
        <Style x:Key="DlgScrollThumb" TargetType="{x:Type Thumb}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border Background="{DynamicResource ScrollThumb}" CornerRadius="3" Margin="2"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DlgScrollPageBtn" TargetType="{x:Type RepeatButton}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Border Background="Transparent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Width" Value="6"/>
            <Setter Property="MinWidth" Value="6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Border Background="Transparent">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource DlgScrollPageBtn}" Command="ScrollBar.PageUpCommand"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource DlgScrollThumb}"/>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource DlgScrollPageBtn}" Command="ScrollBar.PageDownCommand"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Section title -->
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="Foreground"  Value="{DynamicResource TextMuted}"/>
            <Setter Property="FontSize"    Value="10"/>
            <Setter Property="FontWeight"  Value="Bold"/>
        </Style>

        <!-- Field label -->
        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="Foreground"  Value="{DynamicResource TextSecondary}"/>
            <Setter Property="FontSize"    Value="11"/>
            <Setter Property="FontWeight"  Value="SemiBold"/>
            <Setter Property="Margin"      Value="0,0,0,4"/>
        </Style>

        <!-- Text input -->
        <Style x:Key="FormInput" TargetType="TextBox">
            <Setter Property="Background"    Value="{DynamicResource InputBg}"/>
            <Setter Property="Foreground"    Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush"   Value="{DynamicResource InputBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding"       Value="10,8"/>
            <Setter Property="FontSize"      Value="12"/>
            <Setter Property="CaretBrush"    Value="{DynamicResource TextPrimary}"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Padding="{TemplateBinding Padding}"
                                          Focusable="False"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource InputFocusBorder}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Checkbox style -->
        <Style x:Key="FormCheck" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="CheckBorder"
                                    Width="18" Height="18"
                                    CornerRadius="4"
                                    BorderThickness="1.5"
                                    Background="{DynamicResource InputBg}"
                                    BorderBrush="{DynamicResource InputBorder}"
                                    VerticalAlignment="Center">
                                <Path x:Name="CheckMark"
                                      Data="{StaticResource IconCheck}"
                                      Fill="{DynamicResource AccentFg}"
                                      Width="12" Height="12"
                                      Stretch="Uniform"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      Opacity="0"/>
                            </Border>
                            <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="{DynamicResource AccentBorder}"/>
                                <Setter TargetName="CheckBorder" Property="Background" Value="{DynamicResource AccentBg}"/>
                                <Setter TargetName="CheckMark" Property="Opacity" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="{DynamicResource AccentFg}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary button -->
        <Style x:Key="FormBtn" TargetType="Button">
            <Setter Property="Background"      Value="{DynamicResource BtnBg}"/>
            <Setter Property="Foreground"      Value="{DynamicResource BtnFg}"/>
            <Setter Property="BorderBrush"     Value="{DynamicResource BtnBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding"         Value="12,8"/>
            <Setter Property="FontSize"        Value="12"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background"  Value="{DynamicResource BtnHoverBg}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource BtnHoverBorder}"/>
                                <Setter Property="Foreground"  Value="{DynamicResource BtnHoverFg}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource BtnPressedBg}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Accent save button -->
        <Style x:Key="SaveBtn" TargetType="Button" BasedOn="{StaticResource FormBtn}">
            <Setter Property="Background"  Value="{DynamicResource AccentBg}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBorder}"/>
            <Setter Property="Foreground"  Value="{DynamicResource AccentFg}"/>
            <Setter Property="Padding"     Value="20,8"/>
        </Style>

        <!-- Small icon button -->
        <Style x:Key="SmallIconBtn" TargetType="Button">
            <Setter Property="Background"      Value="{DynamicResource BtnBg}"/>
            <Setter Property="Foreground"      Value="{DynamicResource BtnFg}"/>
            <Setter Property="BorderBrush"     Value="{DynamicResource BtnBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Width"           Value="32"/>
            <Setter Property="Height"          Value="32"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background"  Value="{DynamicResource BtnHoverBg}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource BtnHoverBorder}"/>
                                <Setter Property="Foreground"  Value="{DynamicResource BtnHoverFg}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource BtnPressedBg}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListBox style for services -->
        <Style x:Key="ServiceListBox" TargetType="ListBox">
            <Setter Property="Background" Value="{DynamicResource InputBg}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer VerticalScrollBarVisibility="Auto"
                                          HorizontalScrollBarVisibility="Disabled"
                                          Padding="2">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListBoxItem style -->
        <Style x:Key="ServiceListBoxItem" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Margin" Value="0,1"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="5">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource BtnHoverBg}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentBg}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ═══ Header ═══ -->
        <Border Grid.Row="0"
                Background="{DynamicResource HeaderBg}"
                BorderBrush="{DynamicResource HeaderBorder}"
                BorderThickness="0,0,0,1"
                Padding="20,16">
            <StackPanel Orientation="Horizontal">
                <Border Width="36" Height="36" CornerRadius="10" Margin="0,0,12,0"
                        Background="{DynamicResource DialogIconGradient}">
                    <Path Data="{StaticResource IconServices}"
                          Fill="{DynamicResource DialogIconFill}"
                          Width="18" Height="18"
                          Stretch="Uniform"
                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding Title}"
                               FontSize="15" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimary}"/>
                    <TextBlock Text="Configure services for quick start/stop operations"
                               FontSize="10.5" Foreground="{DynamicResource TextMuted}"
                               Margin="0,2,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- ═══ Main Content ═══ -->
        <Grid Grid.Row="1" Margin="20,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Profile info fields -->
            <StackPanel Grid.Row="0" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Name -->
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="PROFILE NAME" Style="{StaticResource SectionTitle}" Margin="0,0,0,6"/>
                        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource FormInput}"/>
                    </StackPanel>

                    <!-- Description -->
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="DESCRIPTION (OPTIONAL)" Style="{StaticResource SectionTitle}" Margin="0,0,0,6"/>
                        <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource FormInput}"/>
                    </StackPanel>
                </Grid>

                <!-- Default checkbox -->
                <CheckBox Style="{StaticResource FormCheck}"
                          IsChecked="{Binding IsDefault}"
                          Margin="0,12,0,0">
                    <TextBlock>
                        <Run Text="Set as default profile"/>
                        <Run Text=" (loads automatically on startup)" Foreground="{DynamicResource TextMuted}" FontSize="10"/>
                    </TextBlock>
                </CheckBox>
            </StackPanel>

            <!-- Service selector -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Available Services -->
                <DockPanel Grid.Column="0">
                    <StackPanel DockPanel.Dock="Top" Margin="0,0,0,8">
                        <TextBlock Text="AVAILABLE SERVICES" Style="{StaticResource SectionTitle}"/>
                        <!-- Search box -->
                        <Border Background="{DynamicResource InputBg}"
                                BorderBrush="{DynamicResource InputBorder}"
                                BorderThickness="1" CornerRadius="6"
                                Height="32" Margin="0,6,0,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Path Grid.Column="0"
                                      Data="{StaticResource IconSearch}"
                                      Fill="{DynamicResource TextMuted}"
                                      Width="13" Height="13"
                                      Stretch="Uniform"
                                      Margin="10,0,6,0"
                                      VerticalAlignment="Center"/>
                                <TextBox Grid.Column="1"
                                         Text="{Binding ServiceSearchText, UpdateSourceTrigger=PropertyChanged}"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         Foreground="{DynamicResource TextPrimary}"
                                         CaretBrush="{DynamicResource TextPrimary}"
                                         Padding="4,8,10,8"
                                         FontSize="12"
                                         VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </StackPanel>
                    <ListBox ItemsSource="{Binding FilteredAvailableServices}"
                             SelectedItem="{Binding SelectedAvailableService}"
                             Style="{StaticResource ServiceListBox}"
                             ItemContainerStyle="{StaticResource ServiceListBoxItem}"
                             MouseDoubleClick="AvailableServices_DoubleClick">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                                        <TextBlock Text="{Binding DisplayName}"
                                                   FontSize="12"
                                                   Foreground="{DynamicResource TextPrimary}"
                                                   VerticalAlignment="Center"/>
                                        <Border Visibility="{Binding IsNodeApp, Converter={StaticResource BoolToVis}}"
                                                Background="{DynamicResource TypeBadgeBg}"
                                                CornerRadius="3"
                                                Padding="4,1"
                                                Margin="6,0,0,0"
                                                ToolTip="NodeApp services cannot be auto-started/stopped">
                                            <TextBlock Text="Manual" FontSize="9" Foreground="{DynamicResource TypeBadgeFg}"/>
                                        </Border>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding TypeName}"
                                               FontSize="10"
                                               Foreground="{DynamicResource TextMuted}"
                                               VerticalAlignment="Center"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>

                <!-- Add/Remove buttons -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0">
                    <Button Style="{StaticResource SmallIconBtn}"
                            Command="{Binding AddServiceCommand}"
                            ToolTip="Add selected service"
                            Margin="0,0,0,6">
                        <Path Data="{StaticResource IconChevronRight}"
                              Fill="{DynamicResource BtnFg}"
                              Width="14" Height="14"
                              Stretch="Uniform"/>
                    </Button>
                    <Button Style="{StaticResource SmallIconBtn}"
                            Command="{Binding AddAllFilteredServicesCommand}"
                            ToolTip="Add all filtered services"
                            Margin="0,0,0,12">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="{StaticResource IconChevronRight}"
                                  Fill="{DynamicResource BtnFg}"
                                  Width="10" Height="10"
                                  Stretch="Uniform"
                                  Margin="0,0,-5,0"/>
                            <Path Data="{StaticResource IconChevronRight}"
                                  Fill="{DynamicResource BtnFg}"
                                  Width="10" Height="10"
                                  Stretch="Uniform"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource SmallIconBtn}"
                            Command="{Binding RemoveServiceCommand}"
                            ToolTip="Remove selected service"
                            Margin="0,0,0,6">
                        <Path Data="{StaticResource IconChevronRight}"
                              Fill="{DynamicResource BtnFg}"
                              Width="14" Height="14"
                              Stretch="Uniform"
                              RenderTransformOrigin="0.5,0.5">
                            <Path.RenderTransform>
                                <ScaleTransform ScaleX="-1"/>
                            </Path.RenderTransform>
                        </Path>
                    </Button>
                    <Button Style="{StaticResource SmallIconBtn}"
                            Command="{Binding RemoveAllServicesCommand}"
                            ToolTip="Remove all services">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="{StaticResource IconChevronRight}"
                                  Fill="{DynamicResource BtnFg}"
                                  Width="10" Height="10"
                                  Stretch="Uniform"
                                  RenderTransformOrigin="0.5,0.5"
                                  Margin="0,0,-5,0">
                                <Path.RenderTransform>
                                    <ScaleTransform ScaleX="-1"/>
                                </Path.RenderTransform>
                            </Path>
                            <Path Data="{StaticResource IconChevronRight}"
                                  Fill="{DynamicResource BtnFg}"
                                  Width="10" Height="10"
                                  Stretch="Uniform"
                                  RenderTransformOrigin="0.5,0.5">
                                <Path.RenderTransform>
                                    <ScaleTransform ScaleX="-1"/>
                                </Path.RenderTransform>
                            </Path>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- Profile Services -->
                <DockPanel Grid.Column="2">
                    <StackPanel DockPanel.Dock="Top" Margin="0,0,0,8">
                        <Grid>
                            <TextBlock Text="PROFILE SERVICES" Style="{StaticResource SectionTitle}"/>
                            <TextBlock HorizontalAlignment="Right"
                                       FontSize="10"
                                       Foreground="{DynamicResource TextMuted}">
                                <Run Text="{Binding ProfileServices.Count, Mode=OneWay}"/>
                                <Run Text="selected"/>
                            </TextBlock>
                        </Grid>
                        <!-- Spacer to align with search box -->
                        <Border Height="38" Background="Transparent"/>
                    </StackPanel>
                    <ListBox ItemsSource="{Binding ProfileServices}"
                             SelectedItem="{Binding SelectedProfileService}"
                             Style="{StaticResource ServiceListBox}"
                             ItemContainerStyle="{StaticResource ServiceListBoxItem}"
                             MouseDoubleClick="ProfileServices_DoubleClick">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                                        <TextBlock Text="{Binding DisplayName}"
                                                   FontSize="12"
                                                   Foreground="{DynamicResource TextPrimary}"
                                                   VerticalAlignment="Center"/>
                                        <Border Visibility="{Binding IsNodeApp, Converter={StaticResource BoolToVis}}"
                                                Background="{DynamicResource TypeBadgeBg}"
                                                CornerRadius="3"
                                                Padding="4,1"
                                                Margin="6,0,0,0"
                                                ToolTip="NodeApp services cannot be auto-started/stopped">
                                            <TextBlock Text="Manual" FontSize="9" Foreground="{DynamicResource TypeBadgeFg}"/>
                                        </Border>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding TypeName}"
                                               FontSize="10"
                                               Foreground="{DynamicResource TextMuted}"
                                               VerticalAlignment="Center"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>

                <!-- Move up/down buttons -->
                <StackPanel Grid.Column="3" VerticalAlignment="Center" Margin="12,0,0,0">
                    <Button Style="{StaticResource SmallIconBtn}"
                            Command="{Binding MoveServiceUpCommand}"
                            ToolTip="Move up"
                            Margin="0,0,0,6">
                        <Path Data="{StaticResource IconChevronRight}"
                              Fill="{DynamicResource BtnFg}"
                              Width="14" Height="14"
                              Stretch="Uniform"
                              RenderTransformOrigin="0.5,0.5">
                            <Path.RenderTransform>
                                <RotateTransform Angle="-90"/>
                            </Path.RenderTransform>
                        </Path>
                    </Button>
                    <Button Style="{StaticResource SmallIconBtn}"
                            Command="{Binding MoveServiceDownCommand}"
                            ToolTip="Move down">
                        <Path Data="{StaticResource IconChevronRight}"
                              Fill="{DynamicResource BtnFg}"
                              Width="14" Height="14"
                              Stretch="Uniform"
                              RenderTransformOrigin="0.5,0.5">
                            <Path.RenderTransform>
                                <RotateTransform Angle="90"/>
                            </Path.RenderTransform>
                        </Path>
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>

        <!-- ═══ Footer ═══ -->
        <Border Grid.Row="2"
                Background="{DynamicResource HeaderBg}"
                BorderBrush="{DynamicResource HeaderBorder}"
                BorderThickness="0,1,0,0"
                Padding="20,14">
            <Grid>
                <TextBlock VerticalAlignment="Center"
                           Foreground="{DynamicResource TextMuted}"
                           FontSize="10">
                    <Run Text="Note: NodeApp services cannot be auto-started/stopped"/>
                </TextBlock>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel"
                            Style="{StaticResource FormBtn}"
                            Padding="16,8" Margin="0,0,10,0"
                            Click="OnCancel"/>
                    <Button Style="{StaticResource SaveBtn}"
                            Click="OnSave"
                            IsEnabled="{Binding CanSave}">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="{StaticResource IconCheck}"
                                  Fill="{DynamicResource AccentFg}"
                                  Width="12" Height="12"
                                  Margin="0,0,7,0"
                                  Stretch="Uniform"
                                  VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding SaveButtonText}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
